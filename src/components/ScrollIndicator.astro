<div data-scroll-indicator class="flex flex-col items-center gap-2 select-none" aria-hidden="true">
  <span class="font-mono text-[0.6rem] tracking-[0.25em] uppercase text-text-dim">scroll</span>
  <div class="flex flex-col items-center gap-0.5">
    <div class="w-px h-8 relative overflow-hidden">
      <div class="w-full h-full bg-gradient-to-b from-transparent via-accent-primary/40 to-accent-primary/80" style="animation: scrollLine 2s ease-in-out infinite;"></div>
    </div>
    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-accent-primary/50">
      <polyline points="6 9 12 15 18 9" />
    </svg>
  </div>
</div>

<style>
  @keyframes scrollLine {
    0% { transform: translateY(-100%); opacity: 0; }
    30% { opacity: 1; }
    100% { transform: translateY(100%); opacity: 0; }
  }
</style>